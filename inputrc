# this file configures the readline library used by bash and various other utilities

# hitting <TAB> once will show all possible completions (normally you have
# to hit <TAB> twice)
set show-all-if-ambiguous on

# do not use casesensitive completion
set completion-ignore-case on

# if there are more than 150 possible completions for a word, ask the
# user if he wants to see all of them
#set completion-query-items 150

# Adding this to your /etc/inputrc or ~/.inputrc will result in a character
# being appended to any file-names returned by completion, in much the same
# way as ls -F works.
#set visible-stats on

# Completed names which are symbolic links to
# directories have a slash appended.
#set mark-symlinked-directories on

#"\e[1~": beginning-of-line
#"\e[4~": end-of-line
#"\e[5~": beginning-of-history
#"\e[6~": end-of-history
#"\e[3~": delete-char
#"\e[2~": quoted-insert
#"\e[5C": forward-word
#"\e[5D": backward-word
"\e\e[C": forward-word		# ESC+RightArrow
"\e\e[D": backward-word		# ESC+LeftArrow

# this feature will use the TAB key to cycle through all the possible values for completion
#"\t": menu-complete

#set bell-style visible
#set expand-tilde on
#set convert-meta off
#set input-meta on
#set output-meta on